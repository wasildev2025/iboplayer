// Side-effect CSS imports (e.g. `import "./globals.css"`) are handled by
// Next.js at build time, but TypeScript needs a module declaration to stop
// flagging them as unresolved.
declare module "*.css";
